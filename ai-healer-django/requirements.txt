asgiref==3.11.0
diff-match-patch==20241021
Django==5.0
django-admin-interface==0.32.0
django-colorfield==0.14.0
django-import-export==4.4.0
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
# mysqlclient removed from base requirements (Phase 10): it needs MySQL C
# headers that aren't in the Render Docker image, and Render deploys use
# Postgres via psycopg2-binary. Local Mac dev that wants MySQL can
# `pip install mysqlclient==2.2.7` into its own venv separately.
pillow==12.1.0
PyJWT==2.10.1
python-slugify==8.0.4
sqlparse==0.5.5
tablib==3.9.0
text-unidecode==1.3
typing_extensions==4.15.0

# DOM Extraction and Matching Engine dependencies.
# Phase 10 — sentence-transformers + faiss-cpu removed (pulled in torch,
# ~700 MB, OOM'd Render's 512 MB free tier). matching_engine.py has a
# try/except fallback (see lines 10-20) that switches to scikit-learn's
# TF-IDF when either import fails — semantic ranking is slightly less
# accurate but pipeline works. Add them back for local dev with
# `pip install sentence-transformers==3.3.1 faiss-cpu==1.9.0.post1` if
# you want the SBERT path.
beautifulsoup4==4.13.4
numpy==2.2.2
pandas==2.2.3
scikit-learn==1.6.1

# Phase 3 — dashboard absorbs Streamlit
# django-q2: background task runner for npm gen:testcases / playwright test
# cryptography: Fernet symmetric encryption for Jira API tokens at rest
# redis: broker for django-q2; also the recommended runtime cache
django-q2==1.7.6
cryptography==43.0.1
redis==5.0.8
requests==2.32.3

# Phase 4 — all agents route through OpenAI (Ollama removed).
# See test_generation/llm_backends.py.
openai>=1.50

# Phase 10 — Render deployment: WSGI server, Postgres driver, DB URL parser,
# static-file middleware. mysqlclient was moved OUT of the base requirements
# because it needs MySQL C headers the Render image doesn't ship; the
# DATABASES block reads DATABASE_URL (Postgres) in prod, MySQL block only
# activates when DATABASE_URL is unset (local dev).
gunicorn>=22.0
psycopg2-binary>=2.9
dj-database-url>=2.2
whitenoise>=6.6
